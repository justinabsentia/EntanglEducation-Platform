import React from 'react'
import ReactDOM from 'react-dom/client'
import EntangleduMain from './EntangleduMain.jsx'
import './index.css' // We will create this in Step 3

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EntangleduMain />
  </React.StrictMode>,
)
